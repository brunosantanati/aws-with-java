aws:
  access-key: "abc"
  secret-key: "abc"
  dynamodb:
  #endpoint: "http://localhost:8000"

cloud:
  aws:
    stack:
      auto: false
    region:
      static: us-east-2

spring:
  data:
    dynamodb:
      entity2ddl:
        auto: validate

